- enable signature drag corner to resize
- add markdown features: text entry, line drawing, text highlighting
- add the ability to switch between light and dark display modes. default should follow whatever the OS has set.
- [done] update unsaved changes check when closing file. currently, the check only works when closing a file, not when closing the application.
- add an about menu when the user clicks on the app name in the top left hand corner
- [done] in windows when set as the default pdf reader, double-clicking a pdf opens the app but did not load the file.